package com.owtelse.http

/**
 * All Known Content Coding
 * @see http://www.iana.org/assignments/http-parameters/http-parameters.xhtml#content-coding
 */
object ContentCoding {
  val `compress`	= "compress"
  val `deflate`	= "deflate"
  val `exi`	= "exi"
  val `gzip`	= "gzip"
  val `identity`	= "identity"
  val `pack200-gzip`	="pack200-gzip"
  val `x-compress`	= "x-compress"
  val `x-gzip` = "x-gzip"
}
